function simpleMultiplication(number) {
    // your code........
  // even number (%2 === 0)*8 || (%3 === 0)*9
  if (number % 2 === 0){
    return (number * 8);
  } else {
    return (number * 9);
  }
}