// Sum Numbers
function sum (numbers) {
    let start = 0;
  for(let i = 0; i < numbers.length; i++){
    start +=numbers[i];
  }
    return start
};

/* sum starts at 0
for loop to iterate through array
every iteration sum + element of array


*/