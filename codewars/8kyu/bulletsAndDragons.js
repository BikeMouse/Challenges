function hero(bullets, dragons){
    //Get Coding!
      //more bullets then dragons
      // 2 bullets kill 1 dragon
      if (bullets > dragons && bullets >= (2*dragons)){
        return true
        } else {
          return false
        }
    }